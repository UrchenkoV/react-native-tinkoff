import { View, Text, StyleSheet } from "react-native";
import React, { FC } from "react";

const SupportHeader: FC = () => {
  return (
    <View>
      <Text>SupportHeader</Text>
    </View>
  );
};

export default SupportHeader;
